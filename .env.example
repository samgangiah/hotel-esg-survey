# --- Required ---------------------------------------------------------------

# Postgres password (used by both esg-app and esg-db).
POSTGRES_PASSWORD=

# Signs session cookies. Rotate via overlap window — never just replace.
# Generate with: openssl rand -hex 32
SESSION_SIGNING_KEY=

# Sole address allowed to log in to /operator.
OPERATOR_EMAIL=samendran@gmail.com

# Base URL the app uses to construct invite links.
APP_URL=https://esg.digitalrain.cloud

# --- Email (Phase 0.D, not required at boot) --------------------------------

RESEND_API_KEY=
RESEND_FROM=Hotel ESG Survey <invites@mail.digitalrain.cloud>

# --- File storage (Phase 0.F) -----------------------------------------------

R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_ENDPOINT=

# --- Observability (Phase 0.F) ----------------------------------------------

SENTRY_DSN=
PUSHOVER_USER_KEY=
PUSHOVER_APP_TOKEN=

# --- Set automatically by docker-compose if you don't override ---------------

# DATABASE_URL=postgresql://esg:${POSTGRES_PASSWORD}@esg-db:5432/esg?schema=public
